<project name="scalarizr" default="usage" basedir=".">

<!-- 
	<target name="upload-to-instance">
		<scp 
			file="${package.home}/scalarizr.jar" 
			todir="root@${ec2instance.host}:/opt/scalarizr"
			keyfile="${ec2instance.keyfile}" passphrase="">
			<fileset dir="${package.home}/etc" includes="*" />
		</scp>
	</target>
 -->
 
<!-- 	
	<target name="upload-to-instance-all" depends="upload-to-instance">
		<sshexec host="${ec2instance.host}" username="root" command="mkdir /opt/scalarizr/{etc,lib,perl,logs}" keyfile="${ec2instance.keyfile}" passphrase=""/>
		<scp todir="root@${ec2instance.host}:/opt/scalarizr/lib" keyfile="${ec2instance.keyfile}" passphrase="">
			<fileset dir="${package.home}/lib" includes="*.jar"/>
		</scp>
		<scp todir="root@${ec2instance.host}:/opt/scalarizr/etc" keyfile="${ec2instance.keyfile}" passphrase="">
			<fileset dir="${package.home}/etc" includes="*" />
		</scp>
		<scp todir="root@${ec2instance.host}:/opt/scalarizr/perl" keyfile="${ec2instance.keyfile}" passphrase="">
			<fileset dir="${package.home}/perl" includes="*" />
		</scp>
	</target>
 -->
 	
</project>