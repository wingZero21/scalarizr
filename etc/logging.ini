[loggers]
keys=root

[handlers]
keys=console,emerg

[formatters]
keys=simple

[logger_root]
level=DEBUG
handlers=console,emerg

[handler_console]
class=StreamHandler
level=DEBUG
formatter=simple
args=(sys.stdout,)

[handler_emerg]
class=handlers.SMTPHandler
level=ERROR
formatter=simple
args=('localhost', 'alerts-scalarizr@scalr.net', ['marat@local.webta.net'], 'Scalarizr alert')


[formatter_simple]
format=%(asctime)s - %(levelname)s - %(name)s - %(message)s

